Railway deploy trigger

Verified latest main includes dashboard barWidth helper in app/projects/[id]/dashboard-lite/page.tsx.

If Railway still reports Cannot find name 'barWidth', it is building an older deployment snapshot instead of the latest main branch.

Trigger: deploy after verifying barWidth helper
